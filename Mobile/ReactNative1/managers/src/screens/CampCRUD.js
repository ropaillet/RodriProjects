import React, { Component } from 'react';
import { View, Text } from 'react-native';
import { connect } from 'react-redux';
import {
  AppHeader, Card, CardSection, Input, Button1, Spinner1
} from './common';


class campCRUD extends Component {
  render () {
    return (
      <Card>


      </Card>
    );
  }
}
